#include "Rover.cpp"

#include <iostream>

int main() {

    Rover rover;

    // rover.move_to_position(3, 3);
    // rover.move_to_position(-3, -3);
    // rover.move_to_position(-3, 3);
    // rover.move_to_position(3, -3);
    
    // rover.move_to_position(1, 1);
    // rover.move_to_position(-2, -2);
    // rover.move_to_position(2, 2);
    // rover.move_to_position(-1, -1);
    // rover.move_to_position(2, -2);

    return 0;
}